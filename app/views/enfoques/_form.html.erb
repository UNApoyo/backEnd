<%= form_for(enfoque) do |f| %>
  <% if enfoque.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(enfoque.errors.count, "error") %> prohibited this enfoque from being saved:</h2>

      <ul>
      <% enfoque.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %>
    <%= f.text_field :nombre %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
