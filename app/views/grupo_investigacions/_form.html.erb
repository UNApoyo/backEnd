<%= form_for(grupo_investigacion) do |f| %>
  <% if grupo_investigacion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(grupo_investigacion.errors.count, "error") %> prohibited this grupo_investigacion from being saved:</h2>

      <ul>
      <% grupo_investigacion.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %>
    <%= f.text_field :nombre %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
